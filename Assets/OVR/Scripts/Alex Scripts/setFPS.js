#pragma strict

function Start () {
    // Make the game run as fast as possible
    Application.targetFrameRate = 5;
}

function Update () {
	
}

function Awake () {
    // Make the game run as fast as possible
    Application.targetFrameRate = 5;
}